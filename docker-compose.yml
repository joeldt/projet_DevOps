services:
  app:
    build: .
    container_name: portfolio-joel-final
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    restart: always